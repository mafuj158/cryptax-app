export interface SubscribeNewsletterSuccessResponse {
    status: boolean;
    message: string;
    code: number;
}

export interface SubscribeNewsletterErrorResponse {
    status: boolean;
    message: string;
    code: number;
}